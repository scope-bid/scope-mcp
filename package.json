{
  "name": "@scope-bid/mcp-server",
  "version": "0.1.0",
  "private": true,
  "description": "Scope MCP server - dispatch litigation work to legal-services vendors from any AI legal workflow.",
  "type": "module",
  "main": "dist/server.js",
  "bin": {
    "scope-mcp": "dist/server.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "tsx server.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.5.0"
  }
}
